# setupSparkyLinux 项目开发准则

在对本项目进行维护、迁移或重构时，必须遵守以下核心准则：

## 1. 尊重“专家经验”正则表达式 (Expert Regex)
*   **准则**：严禁直接使用通用的“资产查找算法”替换老代码（`legacy/` 目录）中经过人工调研得出的特定正则表达式。
*   **逻辑**：GitHub 仓库的命名完全取决于开发者（如 LocalSend 的 arm64 顺序问题）。老代码中的正则（如 `.*linux-x86-64.*\.deb$`）是作者针对每个仓库的命名规律人工分析后的最优成果。
*   **强制操作**：迁移任何涉及 GitHub 来源的模块时，必须先在 `legacy/` 脚本中搜索关键词，找到其专属的匹配正则并继承。

## 2. 功能对齐优先于技术升级 (Parity over Technology)
*   **准则**：在引入底层新技术（如 API 抓取、JSON 过滤、版本对比）之前，必须首先确保旧有的所有核心逻辑、下载源和特殊处理在现有架构下完全对齐。
*   **原则**：先复刻功能，再讨论重构。

## 3. 抵制过度通用化 (Avoid Over-Generalization)
*   **准则**：如果一个软件需要特定的安装源（如 Rclone 官方脚本）、特定的锁版本操作（如 WPS 的 `apt-mark hold`）或特殊的依赖补丁，必须在模块内显式保留这些逻辑。
*   **底线**：不要为了让库函数看起来“万能”而强行剥离模块需要的特殊性。

## 4. 架构感知 (Arch-Awareness)
*   **准则**：底层下载函数必须具备架构感知（x86_64 vs arm64），且在没有明确指定匹配正则时，能够根据当前系统架构进行优先级筛选，而不是盲目抓取第一个匹配项。
